<![CDATA[import type { NextApiRequest, NextApiResponse } from "next";
import { FieldValue, Timestam
...
 err instanceof Error ? err.message : "Unknown error" });
  }
}

export { detectType };
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 2024 chars.]