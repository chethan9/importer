<![CDATA[import type { NextApiRequest, NextApiResponse } from "next";
import { withAdmin } from "@/l
...
sage: err instanceof Error ? err.message : "Unknown error" });
  }
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 945 chars.]