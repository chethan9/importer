<![CDATA[import { useCallback, useState } from "react";
import { SEO } from "@/c
...
       </div>
        <HistorySheet
          open={historyOpen}
          onOpenChange={setHistoryOpen}
          onResumeRequest={handleResumeRequest}
        />
      </div>
    </>
  );
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 3926 chars.]