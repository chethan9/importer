<![CDATA[const SA_KEY = "fb-importer:serviceAccount";

export type ServiceAccount = {
  project_id:
...tions({ serviceAccount: sa, ops });
  return data.results as Array<{ path: string; ok: boolean; error?: string }>;
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 3149 chars.]