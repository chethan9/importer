<![CDATA[
import { Check, Link2, Database, ArrowRightLeft, Upload, PlayCircle } from "lucide-react";
import { cn } f
...
           {step.label}
            </span>
          </button>
        );
      })}
    </ol>
  );
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 2146 chars.]